{
  "name": "perm-exercise",
  "version": "1.0.0",
  "description": "An alghoritm that accepts a single string as input, and that returns a list of English words that can be created using some combination of the letters in the input string.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "coverage": "pnpm build && vitest run --coverage",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "Michele Ursino",
  "license": "MIT",
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.9.0",
    "@types/prompt-sync": "^4.2.3",
    "@vitest/coverage-istanbul": "^0.34.6",
    "@vitest/coverage-v8": "^0.34.6",
    "bun-types": "^1.0.11",
    "typescript": "^5.2.2",
    "vitest": "^0.34.6"
  },
  "dependencies": {
    "fs-extra": "^11.1.1",
    "prompt-sync": "^4.2.0"
  }
}
